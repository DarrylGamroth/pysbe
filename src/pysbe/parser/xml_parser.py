"""XML parser scaffolding for future schema model generation."""

from __future__ import annotations

from pathlib import Path


def parse_schema(path: str | Path) -> None:
    """Parse an XML schema file into an internal schema model (Phase 1)."""

    del path
    raise NotImplementedError("Schema parsing is implemented in Phase 1")
